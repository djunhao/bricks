<!DOCTYPE html>
<html>
<head>
    <title>some more testing</title>
    <meta charset="utf-8"/>
    <link type="text/css" href="/bootstrap/css/bootstrap.min.css"/>
</head>
<body>
<h1>${message}</h1>
<p>${user.realName} - ${user.role.name}</p>
<select>
<#list roles as arole>
<#if arole.name == user.role.name>
<option value="${arole.name}" selected>${arole.name}</option>
<#else>
<option value="${arole.name}">${arole.name}</option>
</#if>
</#list>
</select>
<script type="text/javascript" charset="utf-8" src="/bootstrap/js/bootstrap.min.js"></script>
</body>
</html>